# Copy to secrets.yaml for local builds. Production builds substitute these via
# ESPHome Build Dashboard's own secrets mechanism — see homelab-esphome/CLAUDE.md.

wifi_ssid: "your-ssid"
wifi_password: "your-password"
api_encryption_key: "32-byte-base64-encryption-key=="
ota_password: "your-ota-password"
